1. สร้าง database container (ให้ insert ข้อมูลผ่าน script.sql ได้)
sudo docker run
sudo docker run -d --name mysqldb -p 13306:3306 -v ${pwd}/db/my.cnf:/etc/my.cnf -v ${pwd}/data,dst=/var/lib/mysql -e MYSQL_ROOT_PASSWORD='123' mysql/mysql-server:8.0